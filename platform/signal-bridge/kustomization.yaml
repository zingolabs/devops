apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - signal-cli.yaml
  - webhook-bridge.yaml
  # After sealed-secrets controller is running, generate and add:
  # - sealed-webhook-config.yaml
