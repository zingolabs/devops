apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - signal-cli.yaml
  - webhook-bridge.yaml
  - webhook-config.yaml
  - sealed-webhook-secrets.yaml
  - sealed-signal-credentials.yaml
