apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The deployment-gate poller (cluster-driven monitor). The `deployment-gate`
# WorkflowTemplate (workflow.yaml) is the real validation the poller will submit
# once the connection is proven; kept out of resources until then.
resources:
  - poller.yaml
