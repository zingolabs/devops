apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - rbac.yaml
  - snapshot-golden.yaml
  - seed-zebra-state-cache.yaml
  - build-zaino.yaml
  - buildkit-cache-pvc.yaml
  - deploy-ephemeral.yaml
  - deploy-tryout.yaml
  - serve-zaino.yaml
  - update-zaino.yaml
  - cleanup-ephemeral.yaml
  - bench-zaino.yaml
  - ephemeral-values.yaml
  - zebra-release-watch.yaml
