apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - rbac.yaml
  - snapshot-golden.yaml
  - deploy-ephemeral.yaml
  - deploy-tryout.yaml
  - cleanup-ephemeral.yaml
  - ephemeral-values.yaml
