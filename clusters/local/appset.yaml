# Unified ApplicationSet for local cluster
# Reads from both platform/defs/*.yaml and domain/defs/*.yaml
# Handles two app types:
#   - kustomize: path-based (default)
#   - helm-git: multi-source helm from git repo

apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cluster
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=zero"]
  generators:
    - git:
        repoURL: https://github.com/zingolabs/devops
        revision: dev
        files:
          - path: "platform/defs/*.yaml"
    - git:
        repoURL: https://github.com/zingolabs/devops
        revision: dev
        files:
          - path: "domain/defs/*.yaml"
  template:
    metadata:
      name: '{{.name}}'
      namespace: argocd
    spec:
      project: default
      {{- if eq .type "helm-git" }}
      sources:
        - repoURL: '{{.chartRepo}}'
          path: '{{.chartPath}}'
          targetRevision: '{{.chartRevision}}'
          helm:
            releaseName: '{{.name}}'
            valueFiles:
              - $values/{{ .valuesFile | replace "ENV" "local" }}
        - repoURL: https://github.com/zingolabs/devops
          targetRevision: dev
          ref: values
      {{- else }}
      source:
        repoURL: https://github.com/zingolabs/devops
        path: '{{.path}}'
        targetRevision: dev
      {{- end }}
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{.namespace}}'
      {{- with .ignoreDifferences }}
      ignoreDifferences:
        {{- range . }}
        - group: {{ .group }}
          kind: {{ .kind }}
          jsonPointers:
            {{- range .jsonPointers }}
            - {{ . }}
            {{- end }}
        {{- end }}
      {{- end }}
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          {{- with .syncOptions }}
          {{- range . }}
          - {{ . }}
          {{- end }}
          {{- end }}
